name: dayli-report

on:
    schedule:
        - cron: '1 * * * *'
    workflow_dispatch:
        

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: first-step
            run: echo "hello world"
